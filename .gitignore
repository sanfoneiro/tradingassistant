node_modules/
.next/
out/
build/
.env
.env*.local

# Bearer token the skills and agents read at runtime. Never commit — this
# repository is public.
.agent-token
*.token
.DS_Store
*.pem
npm-debug.log*
.vercel
next-env.d.ts
*.tsbuildinfo

# Quarantined stray copy, pending deletion — contains a nested .git
_to_delete/

# Patch files delivered into the repo root, applied then discarded
*.patch

# Local Claude Code settings — machine-specific, and this repo is public
.claude/settings.local.json

# Database snapshots (npm run db:snapshot). These contain the account balance,
# open positions, fills and P/L — and this repository is PUBLIC. The snapshot
# is a local revert point, not a committed one.
backups/
